EMAIL_ACCOUNTS:
  JOB:
    SMTP_SERVER: smtp.hostinger.com
    SMTP_PORT: '465'
    SMTP_USERNAME: ${SMTP_JOB_USERNAME}
    SMTP_PASSWORD: ${SMTP_JOB_PASSWORD}

  COURSE:
    SMTP_SERVER: smtp.hostinger.com
    SMTP_PORT: '465'
    SMTP_USERNAME: ${SMTP_COURSE_USERNAME}
    SMTP_PASSWORD: ${SMTP_COURSE_PASSWORD}
